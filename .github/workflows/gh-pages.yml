name: Deploy Pages

on:
  push:
    branches: ["main"]

permissions:
  contents: read
  pages: write
  id-token: write

jobs:
  deploy-gh-pages:
    uses: Abhinav5383/gh-workflows/.github/workflows/vite-gh-pages.yml@master
    with:
      project_loc: "."
      build_dir: "dist"